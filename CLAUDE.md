# Claude Code Instructions

Read @AGENTS.md for all project instructions.

## Agent skills

### Issue tracker

Issues and PRDs live as GitHub issues in `xO-xN/PNDS-App` (via the `gh` CLI). See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root (created lazily by `/domain-modeling`). See `docs/agents/domain.md`.
