{
  "name": "multichannel-signal-generator",
  "version": "1.0.0",
  "private": true,
  "description": "PNDS utility project: 16-ch sine wave test for Internal channel checks.",
  "main": "server.js",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/osc.js && node --check lib/audio-engine.js && node --check lib/qr.js && node --check lib/network.js",
    "test": "node --test"
  },
  "dependencies": {
    "qrcode": "^1.5.4"
  }
}
