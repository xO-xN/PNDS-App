{
  "name": "multichannel-tone-test",
  "version": "1.0.0",
  "private": true,
  "description": "PNDS utility project: 16 independent mono test tones for Internal channel checks.",
  "main": "server.js",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check lib/osc.js && node --check lib/audio-engine.js",
    "test": "node --test"
  }
}
